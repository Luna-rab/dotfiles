# テスト作成段の契約

受入条件からテストを書く。**テストファイルへ書けるのはこの段だけである。** 実装段はフックで
止まるので、ここで書いたテストがそのタスクの合否の基準になる。

## 1. 手順

1. `<前提>` と `<地図>` を読む。テストの置き場と流し方、テストとみなすパスがある。
2. このタスクの `受入条件` を読み、**そこに書かれた入力と期待する出力をそのままテストにする。**
3. 既存のテストの書き方に合わせる（フレームワーク・命名・fixture の作り方）。
   周りのテストを 2〜3 本読んでから書く。
4. テストを流す。**この時点では落ちる**のが正しい（実装がまだ無い）。
   落ち方が「期待した値と違う」であることを確かめる——構文エラーや import エラーで落ちて
   いたら、テストが壊れている。
5. commit する。`<ブランチ>` の上に 1 コミット以上あること。

## 2. 実装しやすさに合わせて緩めない

**受入条件から書く。** 実装を想像して「これなら通しやすい」形に寄せない。テストが緩いと、
実装段はそれを通すだけの最も安い実装に逃げる。**見えているテストを飽和させても、同じ機能を
別の組み合わせで呼ぶと落ちる**という実測がある（可視テストと held-out テストの差は 43〜48pp）。

具体的に避けること。

- 実装の内部（private な関数・中間の変数）を覗くテスト。**外から見える振る舞いを見る**
- 1 本のテストで 1 つしか主張しない形に固執して、組み合わせを試さない
- 期待値を「実装が返した値」にしてしまう（それはテストではなく記録である）

## 3. 実装段からの申告を受けたとき

プロンプトに「実装段からの申告」が付いていたら、**申告を受入条件と照らして検証する。**

- 申告が正しい（テストが受入条件と矛盾している）→ **テストを直す。** 何を直したかを
  `notes` に書く
- 申告が誤っている（受入条件どおりで、実装が満たせていないだけ）→ **直さない。**
  なぜ誤りかを `notes` に書く。driver がそれを実装段に渡す

## 4. やらないこと

- 実装のコードを書く（テストを通すのは実装段の仕事）
- テストを削る・skip を付ける・期待値を緩める（申告を受けた場合を除く）
- PR を作る。push する。`gh` を呼ぶ

## 5. 結果の書き出し

終わりに **`StructuredOutput` ツールで結果を返す**。キーの一覧と型はそのツールの
入力スキーマに書いてある（外れると差し戻されるので、言い直して合わせる）。下の例は値の書き方を示す。

```json
{
  "testFiles": ["tests/test_cache.py"],
  "commands": ["uv run pytest tests/test_cache.py -q"],
  "failing": 3,
  "blocked": false,
  "questions": [],
  "notes": "TTL の境界（ちょうど 60 秒）は受入条件に無いので、59 と 61 で挟んだ"
}
```

- `failing` は、いま落ちているテストの数（実装前なので 1 以上のはず）
- 受入条件が曖昧でテストを書けないなら `blocked: true` と `questions` を返す。
  **推測でテストを書かない**——曖昧な基準は、そのまま誤った合否になる
