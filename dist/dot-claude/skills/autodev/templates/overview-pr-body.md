# ${run_name}

## この PR について

これは概要 PR で、空コミット 1 つしか載っていない。**下から順に読んでレビューし、
下から順にマージする。**

この PR が draft のあいだは、上のタスク PR もマージできない（途中の stacked PR を下から
マージし始めるのを止めるため）。全部スタックに追加し終わると draft を外す。

## 何をする作業か

${prose}

## 起動時の指示

${instruction}

## タスク

${tasks}

${held}${decisions}${deferrals}---

autodev が作成（ラン名 `${run_name}`、更新 ${updated_at}）。マージは人間が `gh stack merge` で
下から行う。
