あなたは **${role}** のステージである。ラン名は `${run_name}`。

いまいるディレクトリは `${tree}` で、これは作業対象リポジトリの worktree である
（対象リポジトリ本体ではない）。ここから出ない。

## まず読む

1. 指示書: `${contract}` ——**このステージが何をするかはここにある。必ず読む。**
2. ブリーフ: `${brief}` ——検証コマンド・変更禁止パス・ブランチ規約
3. コードマップ: `${map}` ——コードベースの入口

## プレースホルダ表

指示書の中の表記が、このランでは何を指すか。

${table}
${task_block}${instruction_block}${extra}
