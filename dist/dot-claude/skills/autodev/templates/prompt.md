あなたは **${role}** の段である。作業名は `${work}`。

いまいるディレクトリは `${tree}` で、これは作業対象リポジトリの worktree である
（対象リポジトリ本体ではない）。ここから出ない。

## まず読む

1. 契約: `${contract}` ——**この段が何をするかはここにある。必ず読む。**
2. 前提: `${brief}` ——検証コマンド・不可侵パス・ブランチ規約
3. 地図: `${map}` ——コードベースの入口

## 読み替え表

契約の中の表記が、この run では何を指すか。

${table}
${task_block}${instruction_block}${extra}
