# ${work}

## この PR について

これは stacked PR の土台で、空コミット 1 つしか載っていない。**下から順に読んでレビューし、
下から順にマージする。**

この PR が draft のあいだは、上に積んだ PR もマージできない（途中の stacked PR を下から
マージし始めるのを止めるため）。全部積み終わると draft を外す。

## 何をする作業か

${prose}

## 起動時の指示

${instruction}

## タスク

${tasks}

${held}${decisions}${deferrals}---

autodev が作成（作業名 `${work}`、更新 ${updated_at}）。マージは人間が `gh stack merge` で
下から行う。
